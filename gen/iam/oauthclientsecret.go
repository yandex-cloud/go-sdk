// Code generated by sdkgen. DO NOT EDIT.

// nolint
package iam

import (
	"context"

	"google.golang.org/grpc"

	iam "github.com/yandex-cloud/go-genproto/yandex/cloud/iam/v1"
	"github.com/yandex-cloud/go-genproto/yandex/cloud/operation"
)

//revive:disable

// OAuthClientSecretServiceClient is a iam.OAuthClientSecretServiceClient with
// lazy GRPC connection initialization.
type OAuthClientSecretServiceClient struct {
	getConn func(ctx context.Context) (*grpc.ClientConn, error)
}

// Create implements iam.OAuthClientSecretServiceClient
func (c *OAuthClientSecretServiceClient) Create(ctx context.Context, in *iam.CreateOAuthClientSecretRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return iam.NewOAuthClientSecretServiceClient(conn).Create(ctx, in, opts...)
}

// Delete implements iam.OAuthClientSecretServiceClient
func (c *OAuthClientSecretServiceClient) Delete(ctx context.Context, in *iam.DeleteOAuthClientSecretRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return iam.NewOAuthClientSecretServiceClient(conn).Delete(ctx, in, opts...)
}

// Get implements iam.OAuthClientSecretServiceClient
func (c *OAuthClientSecretServiceClient) Get(ctx context.Context, in *iam.GetOAuthClientSecretRequest, opts ...grpc.CallOption) (*iam.OAuthClientSecret, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return iam.NewOAuthClientSecretServiceClient(conn).Get(ctx, in, opts...)
}

// List implements iam.OAuthClientSecretServiceClient
func (c *OAuthClientSecretServiceClient) List(ctx context.Context, in *iam.ListOAuthClientSecretsRequest, opts ...grpc.CallOption) (*iam.ListOAuthClientSecretsResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return iam.NewOAuthClientSecretServiceClient(conn).List(ctx, in, opts...)
}

type OAuthClientSecretIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *OAuthClientSecretServiceClient
	request *iam.ListOAuthClientSecretsRequest

	items []*iam.OAuthClientSecret
}

func (c *OAuthClientSecretServiceClient) OAuthClientSecretIterator(ctx context.Context, req *iam.ListOAuthClientSecretsRequest, opts ...grpc.CallOption) *OAuthClientSecretIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &OAuthClientSecretIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *OAuthClientSecretIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.List(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.OauthClientSecrets
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *OAuthClientSecretIterator) Take(size int64) ([]*iam.OAuthClientSecret, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*iam.OAuthClientSecret

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *OAuthClientSecretIterator) TakeAll() ([]*iam.OAuthClientSecret, error) {
	return it.Take(0)
}

func (it *OAuthClientSecretIterator) Value() *iam.OAuthClientSecret {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *OAuthClientSecretIterator) Error() error {
	return it.err
}
