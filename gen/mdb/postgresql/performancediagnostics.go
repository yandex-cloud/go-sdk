// Code generated by sdkgen. DO NOT EDIT.

// nolint
package postgresql

import (
	"context"

	"google.golang.org/grpc"

	postgresql "github.com/yandex-cloud/go-genproto/yandex/cloud/mdb/postgresql/v1"
)

//revive:disable

// PerformanceDiagnosticsServiceClient is a postgresql.PerformanceDiagnosticsServiceClient with
// lazy GRPC connection initialization.
type PerformanceDiagnosticsServiceClient struct {
	getConn func(ctx context.Context) (*grpc.ClientConn, error)
}

// ListRawSessionStates implements postgresql.PerformanceDiagnosticsServiceClient
func (c *PerformanceDiagnosticsServiceClient) ListRawSessionStates(ctx context.Context, in *postgresql.ListRawSessionStatesRequest, opts ...grpc.CallOption) (*postgresql.ListRawSessionStatesResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return postgresql.NewPerformanceDiagnosticsServiceClient(conn).ListRawSessionStates(ctx, in, opts...)
}

type PerformanceDiagnosticsRawSessionStatesIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *PerformanceDiagnosticsServiceClient
	request *postgresql.ListRawSessionStatesRequest

	items []*postgresql.SessionState
}

func (c *PerformanceDiagnosticsServiceClient) PerformanceDiagnosticsRawSessionStatesIterator(ctx context.Context, req *postgresql.ListRawSessionStatesRequest, opts ...grpc.CallOption) *PerformanceDiagnosticsRawSessionStatesIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &PerformanceDiagnosticsRawSessionStatesIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *PerformanceDiagnosticsRawSessionStatesIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.ListRawSessionStates(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.SessionStates
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *PerformanceDiagnosticsRawSessionStatesIterator) Take(size int64) ([]*postgresql.SessionState, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*postgresql.SessionState

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *PerformanceDiagnosticsRawSessionStatesIterator) TakeAll() ([]*postgresql.SessionState, error) {
	return it.Take(0)
}

func (it *PerformanceDiagnosticsRawSessionStatesIterator) Value() *postgresql.SessionState {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *PerformanceDiagnosticsRawSessionStatesIterator) Error() error {
	return it.err
}

// ListRawStatements implements postgresql.PerformanceDiagnosticsServiceClient
func (c *PerformanceDiagnosticsServiceClient) ListRawStatements(ctx context.Context, in *postgresql.ListRawStatementsRequest, opts ...grpc.CallOption) (*postgresql.ListRawStatementsResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return postgresql.NewPerformanceDiagnosticsServiceClient(conn).ListRawStatements(ctx, in, opts...)
}

type PerformanceDiagnosticsRawStatementsIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *PerformanceDiagnosticsServiceClient
	request *postgresql.ListRawStatementsRequest

	items []*postgresql.QueryStatement
}

func (c *PerformanceDiagnosticsServiceClient) PerformanceDiagnosticsRawStatementsIterator(ctx context.Context, req *postgresql.ListRawStatementsRequest, opts ...grpc.CallOption) *PerformanceDiagnosticsRawStatementsIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &PerformanceDiagnosticsRawStatementsIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *PerformanceDiagnosticsRawStatementsIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.ListRawStatements(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.Statements
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *PerformanceDiagnosticsRawStatementsIterator) Take(size int64) ([]*postgresql.QueryStatement, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*postgresql.QueryStatement

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *PerformanceDiagnosticsRawStatementsIterator) TakeAll() ([]*postgresql.QueryStatement, error) {
	return it.Take(0)
}

func (it *PerformanceDiagnosticsRawStatementsIterator) Value() *postgresql.QueryStatement {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *PerformanceDiagnosticsRawStatementsIterator) Error() error {
	return it.err
}
