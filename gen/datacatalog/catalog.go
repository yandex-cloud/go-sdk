// Code generated by sdkgen. DO NOT EDIT.

// nolint
package datacatalog

import (
	"context"

	"google.golang.org/grpc"

	datacatalog "github.com/yandex-cloud/go-genproto/yandex/cloud/datacatalog/v1"
	"github.com/yandex-cloud/go-genproto/yandex/cloud/operation"
)

//revive:disable

// CatalogServiceClient is a datacatalog.CatalogServiceClient with
// lazy GRPC connection initialization.
type CatalogServiceClient struct {
	getConn func(ctx context.Context) (*grpc.ClientConn, error)
}

// CreateCatalog implements datacatalog.CatalogServiceClient
func (c *CatalogServiceClient) CreateCatalog(ctx context.Context, in *datacatalog.CreateCatalogRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return datacatalog.NewCatalogServiceClient(conn).CreateCatalog(ctx, in, opts...)
}

// DeleteCatalog implements datacatalog.CatalogServiceClient
func (c *CatalogServiceClient) DeleteCatalog(ctx context.Context, in *datacatalog.DeleteCatalogRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return datacatalog.NewCatalogServiceClient(conn).DeleteCatalog(ctx, in, opts...)
}

// GetCatalog implements datacatalog.CatalogServiceClient
func (c *CatalogServiceClient) GetCatalog(ctx context.Context, in *datacatalog.GetCatalogRequest, opts ...grpc.CallOption) (*datacatalog.Catalog, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return datacatalog.NewCatalogServiceClient(conn).GetCatalog(ctx, in, opts...)
}

// ListCatalogs implements datacatalog.CatalogServiceClient
func (c *CatalogServiceClient) ListCatalogs(ctx context.Context, in *datacatalog.ListCatalogsRequest, opts ...grpc.CallOption) (*datacatalog.ListCatalogsResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return datacatalog.NewCatalogServiceClient(conn).ListCatalogs(ctx, in, opts...)
}

type CatalogCatalogsIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *CatalogServiceClient
	request *datacatalog.ListCatalogsRequest

	items []*datacatalog.Catalog
}

func (c *CatalogServiceClient) CatalogCatalogsIterator(ctx context.Context, req *datacatalog.ListCatalogsRequest, opts ...grpc.CallOption) *CatalogCatalogsIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &CatalogCatalogsIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *CatalogCatalogsIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.ListCatalogs(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.Catalogs
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *CatalogCatalogsIterator) Take(size int64) ([]*datacatalog.Catalog, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*datacatalog.Catalog

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *CatalogCatalogsIterator) TakeAll() ([]*datacatalog.Catalog, error) {
	return it.Take(0)
}

func (it *CatalogCatalogsIterator) Value() *datacatalog.Catalog {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *CatalogCatalogsIterator) Error() error {
	return it.err
}

// UpdateCatalog implements datacatalog.CatalogServiceClient
func (c *CatalogServiceClient) UpdateCatalog(ctx context.Context, in *datacatalog.UpdateCatalogRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return datacatalog.NewCatalogServiceClient(conn).UpdateCatalog(ctx, in, opts...)
}
