// Code generated by sdkgen. DO NOT EDIT.

// nolint
package baremetal

import (
	"context"

	"google.golang.org/grpc"

	baremetal "github.com/yandex-cloud/go-genproto/yandex/cloud/baremetal/v1alpha"
)

//revive:disable

// ConfigurationServiceClient is a baremetal.ConfigurationServiceClient with
// lazy GRPC connection initialization.
type ConfigurationServiceClient struct {
	getConn func(ctx context.Context) (*grpc.ClientConn, error)
}

// Get implements baremetal.ConfigurationServiceClient
func (c *ConfigurationServiceClient) Get(ctx context.Context, in *baremetal.GetConfigurationRequest, opts ...grpc.CallOption) (*baremetal.Configuration, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return baremetal.NewConfigurationServiceClient(conn).Get(ctx, in, opts...)
}

// List implements baremetal.ConfigurationServiceClient
func (c *ConfigurationServiceClient) List(ctx context.Context, in *baremetal.ListConfigurationsRequest, opts ...grpc.CallOption) (*baremetal.ListConfigurationsResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return baremetal.NewConfigurationServiceClient(conn).List(ctx, in, opts...)
}

type ConfigurationIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *ConfigurationServiceClient
	request *baremetal.ListConfigurationsRequest

	items []*baremetal.Configuration
}

func (c *ConfigurationServiceClient) ConfigurationIterator(ctx context.Context, req *baremetal.ListConfigurationsRequest, opts ...grpc.CallOption) *ConfigurationIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &ConfigurationIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *ConfigurationIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.List(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.Configurations
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *ConfigurationIterator) Take(size int64) ([]*baremetal.Configuration, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*baremetal.Configuration

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *ConfigurationIterator) TakeAll() ([]*baremetal.Configuration, error) {
	return it.Take(0)
}

func (it *ConfigurationIterator) Value() *baremetal.Configuration {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *ConfigurationIterator) Error() error {
	return it.err
}
