// Code generated by sdkgen. DO NOT EDIT.

// nolint
package cdn

import (
	"context"

	"google.golang.org/grpc"

	cdn "github.com/yandex-cloud/go-genproto/yandex/cloud/cdn/v1"
	"github.com/yandex-cloud/go-genproto/yandex/cloud/operation"
)

//revive:disable

// ShieldingServiceClient is a cdn.ShieldingServiceClient with
// lazy GRPC connection initialization.
type ShieldingServiceClient struct {
	getConn func(ctx context.Context) (*grpc.ClientConn, error)
}

// Activate implements cdn.ShieldingServiceClient
func (c *ShieldingServiceClient) Activate(ctx context.Context, in *cdn.ActivateShieldingRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cdn.NewShieldingServiceClient(conn).Activate(ctx, in, opts...)
}

// Deactivate implements cdn.ShieldingServiceClient
func (c *ShieldingServiceClient) Deactivate(ctx context.Context, in *cdn.DeactivateShieldingRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cdn.NewShieldingServiceClient(conn).Deactivate(ctx, in, opts...)
}

// Get implements cdn.ShieldingServiceClient
func (c *ShieldingServiceClient) Get(ctx context.Context, in *cdn.GetShieldingDetailsRequest, opts ...grpc.CallOption) (*cdn.ShieldingDetails, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cdn.NewShieldingServiceClient(conn).Get(ctx, in, opts...)
}

// ListAvailableLocations implements cdn.ShieldingServiceClient
func (c *ShieldingServiceClient) ListAvailableLocations(ctx context.Context, in *cdn.ListShieldingLocationsRequest, opts ...grpc.CallOption) (*cdn.ListShieldingLocationsResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cdn.NewShieldingServiceClient(conn).ListAvailableLocations(ctx, in, opts...)
}

type ShieldingAvailableLocationsIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *ShieldingServiceClient
	request *cdn.ListShieldingLocationsRequest

	items []*cdn.ShieldingDetails
}

func (c *ShieldingServiceClient) ShieldingAvailableLocationsIterator(ctx context.Context, req *cdn.ListShieldingLocationsRequest, opts ...grpc.CallOption) *ShieldingAvailableLocationsIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &ShieldingAvailableLocationsIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *ShieldingAvailableLocationsIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.ListAvailableLocations(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.ShieldingLocations
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *ShieldingAvailableLocationsIterator) Take(size int64) ([]*cdn.ShieldingDetails, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*cdn.ShieldingDetails

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *ShieldingAvailableLocationsIterator) TakeAll() ([]*cdn.ShieldingDetails, error) {
	return it.Take(0)
}

func (it *ShieldingAvailableLocationsIterator) Value() *cdn.ShieldingDetails {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *ShieldingAvailableLocationsIterator) Error() error {
	return it.err
}

// Update implements cdn.ShieldingServiceClient
func (c *ShieldingServiceClient) Update(ctx context.Context, in *cdn.UpdateShieldingRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cdn.NewShieldingServiceClient(conn).Update(ctx, in, opts...)
}
