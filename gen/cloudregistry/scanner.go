// Code generated by sdkgen. DO NOT EDIT.

// nolint
package cloudregistry

import (
	"context"

	"google.golang.org/grpc"

	cloudregistry "github.com/yandex-cloud/go-genproto/yandex/cloud/cloudregistry/v1"
	"github.com/yandex-cloud/go-genproto/yandex/cloud/operation"
)

//revive:disable

// ScannerServiceClient is a cloudregistry.ScannerServiceClient with
// lazy GRPC connection initialization.
type ScannerServiceClient struct {
	getConn func(ctx context.Context) (*grpc.ClientConn, error)
}

// Get implements cloudregistry.ScannerServiceClient
func (c *ScannerServiceClient) Get(ctx context.Context, in *cloudregistry.GetScanResultRequest, opts ...grpc.CallOption) (*cloudregistry.ScanResult, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cloudregistry.NewScannerServiceClient(conn).Get(ctx, in, opts...)
}

// GetLast implements cloudregistry.ScannerServiceClient
func (c *ScannerServiceClient) GetLast(ctx context.Context, in *cloudregistry.GetLastScanResultRequest, opts ...grpc.CallOption) (*cloudregistry.ScanResult, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cloudregistry.NewScannerServiceClient(conn).GetLast(ctx, in, opts...)
}

// List implements cloudregistry.ScannerServiceClient
func (c *ScannerServiceClient) List(ctx context.Context, in *cloudregistry.ListScanResultsRequest, opts ...grpc.CallOption) (*cloudregistry.ListScanResultsResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cloudregistry.NewScannerServiceClient(conn).List(ctx, in, opts...)
}

type ScannerIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *ScannerServiceClient
	request *cloudregistry.ListScanResultsRequest

	items []*cloudregistry.ScanResult
}

func (c *ScannerServiceClient) ScannerIterator(ctx context.Context, req *cloudregistry.ListScanResultsRequest, opts ...grpc.CallOption) *ScannerIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &ScannerIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *ScannerIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.List(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.ScanResults
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *ScannerIterator) Take(size int64) ([]*cloudregistry.ScanResult, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*cloudregistry.ScanResult

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *ScannerIterator) TakeAll() ([]*cloudregistry.ScanResult, error) {
	return it.Take(0)
}

func (it *ScannerIterator) Value() *cloudregistry.ScanResult {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *ScannerIterator) Error() error {
	return it.err
}

// ListVulnerabilities implements cloudregistry.ScannerServiceClient
func (c *ScannerServiceClient) ListVulnerabilities(ctx context.Context, in *cloudregistry.ListVulnerabilitiesRequest, opts ...grpc.CallOption) (*cloudregistry.ListVulnerabilitiesResponse, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cloudregistry.NewScannerServiceClient(conn).ListVulnerabilities(ctx, in, opts...)
}

type ScannerVulnerabilitiesIterator struct {
	ctx  context.Context
	opts []grpc.CallOption

	err           error
	started       bool
	requestedSize int64
	pageSize      int64

	client  *ScannerServiceClient
	request *cloudregistry.ListVulnerabilitiesRequest

	items []*cloudregistry.Vulnerability
}

func (c *ScannerServiceClient) ScannerVulnerabilitiesIterator(ctx context.Context, req *cloudregistry.ListVulnerabilitiesRequest, opts ...grpc.CallOption) *ScannerVulnerabilitiesIterator {
	var pageSize int64
	const defaultPageSize = 1000
	pageSize = req.PageSize
	if pageSize == 0 {
		pageSize = defaultPageSize
	}
	return &ScannerVulnerabilitiesIterator{
		ctx:      ctx,
		opts:     opts,
		client:   c,
		request:  req,
		pageSize: pageSize,
	}
}

func (it *ScannerVulnerabilitiesIterator) Next() bool {
	if it.err != nil {
		return false
	}
	if len(it.items) > 1 {
		it.items[0] = nil
		it.items = it.items[1:]
		return true
	}
	it.items = nil // consume last item, if any

	if it.started && it.request.PageToken == "" {
		return false
	}
	it.started = true

	if it.requestedSize == 0 || it.requestedSize > it.pageSize {
		it.request.PageSize = it.pageSize
	} else {
		it.request.PageSize = it.requestedSize
	}

	response, err := it.client.ListVulnerabilities(it.ctx, it.request, it.opts...)
	it.err = err
	if err != nil {
		return false
	}

	it.items = response.Vulnerabilities
	it.request.PageToken = response.NextPageToken
	return len(it.items) > 0
}

func (it *ScannerVulnerabilitiesIterator) Take(size int64) ([]*cloudregistry.Vulnerability, error) {
	if it.err != nil {
		return nil, it.err
	}

	if size == 0 {
		size = 1 << 32 // something insanely large
	}
	it.requestedSize = size
	defer func() {
		// reset iterator for future calls.
		it.requestedSize = 0
	}()

	var result []*cloudregistry.Vulnerability

	for it.requestedSize > 0 && it.Next() {
		it.requestedSize--
		result = append(result, it.Value())
	}

	if it.err != nil {
		return nil, it.err
	}

	return result, nil
}

func (it *ScannerVulnerabilitiesIterator) TakeAll() ([]*cloudregistry.Vulnerability, error) {
	return it.Take(0)
}

func (it *ScannerVulnerabilitiesIterator) Value() *cloudregistry.Vulnerability {
	if len(it.items) == 0 {
		panic("calling Value on empty iterator")
	}
	return it.items[0]
}

func (it *ScannerVulnerabilitiesIterator) Error() error {
	return it.err
}

// Scan implements cloudregistry.ScannerServiceClient
func (c *ScannerServiceClient) Scan(ctx context.Context, in *cloudregistry.ScanRequest, opts ...grpc.CallOption) (*operation.Operation, error) {
	conn, err := c.getConn(ctx)
	if err != nil {
		return nil, err
	}
	return cloudregistry.NewScannerServiceClient(conn).Scan(ctx, in, opts...)
}
